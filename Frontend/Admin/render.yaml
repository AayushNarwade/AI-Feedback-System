services:
  - type: web
    name: fynd-admin-dashboard
    env: python
    region: singapore
    buildCommand: "pip install -r requirements.txt"
    startCommand: "streamlit run streamlit_admin_app.py --server.port $PORT --server.address 0.0.0.0"
