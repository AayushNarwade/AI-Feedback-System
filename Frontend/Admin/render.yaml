services:
  - type: web
    name: fynd-admin-dashboard
    env: python
    region: singapore
    buildCommand: "pip install -r requirements.txt"
    startCommand: "streamlit run admin_dashboard.py --server.port $PORT --server.address 0.0.0.0"
